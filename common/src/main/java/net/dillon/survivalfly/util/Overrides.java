package net.dillon.survivalfly.util;

/**
 * Development overrides for Survival Fly.
 */
public class Overrides {

    /**
     * Determines if the user needs to see the warning message.
     */
    public static boolean seenWarningMessage() {
        return true;
    }
}